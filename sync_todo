4- Ramses and editors
	- sublists sont nommées NEW -> update when set name et shortname ? ou les virer
	- draw progress bar for states
	- test all editors
		Done : users, projects, template steps, template asset groups
	- set default estimations
	- recreate defaults (templates, states...) in included db
	
5A - Move all settings to database (RamSettings object)
  
5- Sync process
	separate thread: send changes since last sync to server, get new data.
		on demand sync
		periodic sync: Sync interval: option, default, 60s
		get online
		get offline (voluntarily)
		on quit (see mainwindow closeEvent)
		
6- simplify daemon & reimplement api

7- Improve Icons

7- Doc

