- DATA/LIST REIMPLEMENTATION
	- Refactor a bit LocalDataInterface // CHK
		- insertObject emit inserted
		- removeObject emit removed
		- restoreObject emit inserted
		- setObjectData emit changed
		- getObjectData
		- use methods above on sync
		- and other specialized methods
	- MODELS
		- RamObjectModel // CHK
			- inherited by DBTableModel // CHK
		- RamOBjectSortFilterProxyModel // WIP
			- filterUuid
			- search name/shortname
			- set as list mode (single column)
		- RamFilterListProxyModel // CHK
			- adds "All items" on top for filter lists
	- DELEGATES
		- RamObjectDelegate // CHK
	- VIEWS
		- RamObjectView // CHK
		- RamObjectComboBox // CHK
		- RamObjectMenu // CHK
		- Specialized views
			- RamStateBox // CHK
	- WIDGETS
		- ObjectManagerWidget // CHK
			- and specialized
		- ObjectListWidget // CHK
	- RamOBjects:
		- roleData override for status (to return completion)
		
	- Update LISTS to use new implementation :
		- State list // OK
		- User list // WIP
			- update user list and its assignlist of projectEditWidget
		- TemplateStep list // WIP
			- projectpage : steptemplatemenu
			- steplistmanagerwidget : createmenu
			- pipelinewidget load stemplatesteps in userChanged
			- pipelinewidget templateStepInserted
			- pipelinewidget templateStepRemoved
		- Project list // WIP
			- user list
			- projecteditwidget user list->setAssignList
		- TemplateAssetGroup list // CHK
			- test create asset groups from template in project editor
		- FileType list // WIP
			- ApplicationEditWidget -> setModel of the 3 file lists // CHK
		- Application list // WIP
			- StepEditWidget -> setModel of the application list
	- Update OBJECTS to use new implementation of SUBLISTS
		- in RamObject: generic list methods; createModel, checkData // CHK
			- derived: create on construct, load if construct from uuid
		- Application // CHK
	- ITEMTABLE implementation
	- Remove old classes
	- Resave templateDB

- SYNC issues
	RamAbstractObject: don't create objects in the db if the shortname is new, but set a var to "to be created"
		create in the db when the shortname is changed
	sync users on creation! (to be able to set the password)
	fullSync on login/setonline (but not create)
	console en mode data crashe au sync -> trop de données? ajouter une limite (enlève les premieres lignes)
	pas possible de changer certains status après qqn d'autre

4- Ramses and editors
	- asset & shot table
		- steps menu filter by type?
	- can't reorder project steps
	- improve console colors
	
5A - Settings refactor
	- Add set settings/get settings in RamAbstractObject
		- a QJSonObject saved in the data "settings"
		- can be used for everything (including pipes!)
		- remove all other use of settings, except for local app settings
		- save a copy of local app settings to the user settings ? at least some of them (like the appearance). On app quit?

6- simplify daemon & reimplement api
	api fixes:
		missing RamFileManager.getFileWithResource(path, resource)
	Update maya addon, and test
		- publish setting do Hide on playback/hide/show/keep as is: curves, locators
		- merge save as and save template
		- build an "item selector" to select an item, and use in both open & save as
		- add a 'recent' list to the open menu
		- update the doc
		- release new dublast for maya
		
	client:
		- enable developer tools (show uuids in objects, and tooltips, etc)
		- status fixes/improvements:
			- open folder should open the right folder
			- refresh icon: white
			- open icon: white

7- change messageboxes icons (create a new class with its own static methods)

7B - DB Clean tool see later
	- Button to clean and vacuum: removes unused objects, sync, vacuum
	- list removed objects by table / type; button to restore/delete permanently
	- button to delete permanently all removed objects
	- button to delete permanently all removed objects (by date, older than)
	- option to auto remove all older objects

7- Doc

9- email confirmation when creating a user, inviting him to set his password

8- a reset password method using the user's email

